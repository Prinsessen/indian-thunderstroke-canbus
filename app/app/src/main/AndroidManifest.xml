<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      From API 31 these two replace the legacy BLUETOOTH/BLUETOOTH_ADMIN pair.
      neverForLocation is what lets the app scan WITHOUT the location permission:
      it promises the scan results are not used to derive position.
    -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Holding the GATT link while the screen is off requires a foreground
         service; Android tears down an activity-held connection immediately. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- A short tick on a gear change and on crossing the redline. Not a runtime
         permission; declaring it is enough. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Springfield">

        <!--
          No screenOrientation lock: the cluster is readable both ways, and the
          layout-land/ variants put the dial beside the numbers in landscape.
          Rotation destroys and recreates the fragments, which is safe precisely
          because no page holds state — everything is re-read from BikeRepository,
          and the BLE link lives in the service, untouched by the rotation.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <activity
            android:name=".DiagnosticsActivity"
            android:exported="false" />

        <activity
            android:name=".AboutActivity"
            android:exported="false" />

        <service
            android:name=".BleService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />
    </application>
</manifest>
