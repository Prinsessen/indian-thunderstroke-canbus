<?xml version="1.0" encoding="utf-8"?>
<!--
  Landscape has width to spare and almost no height, so the secondary strips sit
  BESIDE the dials rather than under them.

  Measured 2026-09-06, and the owner saw it before the arithmetic did: the six
  tell-tale lamps at the foot were the same size as the speed and rev dials. On a
  350 dp landscape screen the tabs take 45, the alert banner 52, and stacking the
  fuel bar, the grips strip and the tell-tale row underneath took another 160 —
  73 % of the screen to chrome, leaving 77 dp for the two instruments the page
  exists for. Both gauges size on min(width, height), so 428 dp of width was
  thrown away to be bound by 77 dp of height.

  Fuel and grips move into a column on the right. The tell-tale row keeps the
  full width along the bottom, because six lamps squeezed into a 200 dp column
  would be worse than the problem being fixed.

  The dials now get about 171 dp instead of 77.

  The side column is 220 dp and the strips inside it are taller than in
  portrait, which costs the dials nothing: they size on min(width, height) and
  height binds them at about 171, so every dp of width above that was already
  being thrown away. FuelBarView scales all of its text off its HEIGHT, so at
  873 x 46 the captions were tiny characters adrift in an enormous strip. That
  was the disproportion, and giving it height rather than width is what fixes it.

  2026-09-14, and the owner saw this one from the saddle too: the ride figure and
  the ignition lamp were still being drawn ON the dial faces, at 0.29 and 0.22 of
  the dial size below the centre, inside a ring of radius 0.40. Nothing
  overlapped by arithmetic, but on a short landscape dial they crowded the two
  numbers the page exists for. Both now live in RideStripView at the foot of the
  side column — the column is where this app already puts a reading you take
  while stopped, and it had the room. The dials have their faces back.

  Column budget: 64 + 10 + 54 + 8 + 52 = 188 dp. The gauge row is about 232 dp
  once the system bars are gone in landscape (see MainActivity.applyImmersive),
  so it fits with room to spare. Check this sum before adding anything else here.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="8dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="horizontal">

        <!-- Speed leads, carrying the gear window; the tachometer sits beside it.
             Each dial draws the turn arrow for its own side, so the indicators
             land at the outer edges rather than huddled in the middle. -->
        <dk.agesen.springfield.GaugeView
            android:id="@+id/gauge"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1" />
        <dk.agesen.springfield.GaugeView
            android:id="@+id/gauge2"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1" />

        <LinearLayout
            android:layout_width="220dp"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center_vertical"
            android:layout_marginStart="8dp">
            <dk.agesen.springfield.FuelBarView
                android:id="@+id/fuel"
                android:layout_width="match_parent"
                android:layout_height="64dp"
                android:layout_marginBottom="10dp" />
            <dk.agesen.springfield.GripsView
                android:id="@+id/grips"
                android:layout_width="match_parent"
                android:layout_height="54dp" />

            <!-- This ride and the ignition lamp, off the dials at last. Long-
                 press the figure to start the ride again, as before. -->
            <dk.agesen.springfield.RideStripView
                android:id="@+id/ridestrip"
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:layout_marginTop="8dp" />
        </LinearLayout>
    </LinearLayout>

    <dk.agesen.springfield.TellTaleView
        android:id="@+id/telltales"
        android:layout_width="match_parent"
        android:layout_height="66dp" />
</LinearLayout>
